def calculate_percentage(base_value, percentage):
 # Calculate the percentage of a base value
 return base_value * percentage / 100

amount = 200  # Base amount to apply the percentage to
discount_percentage = 15  # Percentage value to apply

print(calculate_percentage(amount, discount_percentage)) 